{
  "name": "@mma-finder/db",
  "version": "0.0.0",
  "private": true,
  "description": "Supabase migrations, generated types, and zod schemas. Source of truth for the data model.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "gen": "supabase gen types typescript --linked --schema public > src/types.ts",
    "link": "supabase link --project-ref $SUPABASE_PROJECT_REF",
    "push": "supabase db push",
    "diff": "supabase db diff",
    "status": "supabase migration list",
    "lint": "echo 'noop'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
