{
  "name": "@mma-finder/db",
  "version": "0.0.0",
  "private": true,
  "description": "Supabase migrations, generated types, and zod schemas. Source of truth for the data model.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "gen": "echo 'TODO Phase 0: supabase gen types typescript --linked > src/types.ts'",
    "lint": "echo 'noop'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
